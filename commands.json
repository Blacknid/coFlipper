{
  "protocol": "CFP/1",
  "description": "coFlipper command catalog, on two layers. 'device' = CFP commands sent over the serial port to the Flipper ('cfp <id> <name> [arguments]', see /PROTOCOL.md), executed directly by the hardware. Some device commands (category 'wifi' and 'ble', marked 'requires': 'wifi_dev_board') are served not by the Flipper itself but by an ESP32 Wi-Fi dev board flashed with Marauder firmware and attached to the Flipper's GPIO/UART header; the Flipper's CFP application forwards them to the board. 'agent' = high-level operations, executed on the desktop, which combine one or more device commands with LLM (Gemini) reasoning - this is the original part of the project, not the device commands taken individually, which replicate functions already present in the Flipper Zero / Marauder firmware. Each device command also carries an 'impact' field ('passive' = only listens/reads, 'offensive' = transmits and disrupts other devices, needs the user's authorization) to help the agent reason about consequences. Commands with 'agent_visible': false are for internal use and are not exposed to the model as callable tools.",
  "updated": "2026-07-30",
  "commands": [
    {
      "name": "ping",
      "layer": "device",
      "category": "core",
      "description": "Checks whether the CFP server on the Flipper responds.",
      "args": [],
      "returns": "pong",
      "status": "implemented"
    },
    {
      "name": "info",
      "layer": "device",
      "category": "core",
      "description": "The name/model of the connected Flipper device.",
      "args": [],
      "returns": "the hardware model name",
      "status": "implemented"
    },
    {
      "name": "device.battery",
      "layer": "device",
      "category": "core",
      "description": "The current battery level and whether it is charging.",
      "args": [],
      "returns": "battery percentage, charging state",
      "status": "planned"
    },
    {
      "name": "device.storage.list",
      "layer": "device",
      "category": "core",
      "description": "Lists the files saved on the SD card in a given folder (saved IR/Sub-GHz signals, saved NFC cards, etc.).",
      "args": [
        {
          "name": "path",
          "type": "string",
          "required": true,
          "description": "path on the SD card, e.g. /ext/subghz"
        }
      ],
      "returns": "a list of file names",
      "status": "planned"
    },
    {
      "name": "exit",
      "layer": "device",
      "category": "core",
      "description": "Closes the CFP application on the Flipper, remotely. Needed during development: reinstalling the application requires the previous instance to be closed, and without this command the Back button would have to be pressed physically on every iteration.",
      "args": [],
      "returns": "close confirmation",
      "status": "implemented",
      "agent_visible": false
    },
    {
      "name": "subghz.rssi",
      "layer": "device",
      "category": "subghz",
      "description": "Measures the signal level (RSSI, in dBm) on a given frequency, listening for approximately 100 ms. Values close to -50 dBm indicate a strong signal, those below -90 dBm generally indicate only background noise. It does not decode the signal, only quantifies its presence.",
      "args": [
        {
          "name": "frequency",
          "type": "integer",
          "required": true,
          "description": "frequency in Hz, e.g. 433920000"
        }
      ],
      "returns": "the frequency actually synthesized by the radio (Hz) and the peak RSSI value (dBm)",
      "status": "implemented"
    },
    {
      "name": "subghz.scan",
      "layer": "device",
      "category": "subghz",
      "description": "Scans a list of known frequencies and reports on which of them activity is detected (RSSI above a threshold).",
      "args": [
        {
          "name": "duration_ms",
          "type": "integer",
          "required": false,
          "description": "scan duration, 3000 by default"
        }
      ],
      "returns": "a list of frequencies with a detected signal",
      "status": "planned"
    },
    {
      "name": "subghz.read",
      "layer": "device",
      "category": "subghz",
      "description": "Listens on a specific frequency and tries to identify the protocol of the captured signal (e.g. a gate remote, a weather station).",
      "args": [
        {
          "name": "frequency",
          "type": "integer",
          "required": true,
          "description": "frequency in Hz, e.g. 433920000"
        },
        {
          "name": "timeout_ms",
          "type": "integer",
          "required": false,
          "description": "how long it waits for a signal, 5000 by default"
        }
      ],
      "returns": "the detected protocol (if known), the raw captured data",
      "status": "planned"
    },
    {
      "name": "subghz.replay",
      "layer": "device",
      "category": "subghz",
      "description": "Retransmits a Sub-GHz signal previously saved on the SD card. Intended for testing one's own devices (e.g. one's own gate remote), not for someone else's signals.",
      "args": [
        {
          "name": "file",
          "type": "string",
          "required": true,
          "description": "path to the saved .sub file"
        }
      ],
      "returns": "transmission confirmation",
      "status": "planned"
    },
    {
      "name": "ir.read",
      "layer": "device",
      "category": "infrared",
      "description": "Waits for and decodes the next IR signal received (e.g. a button press on a remote pointed at the Flipper).",
      "args": [
        {
          "name": "timeout_ms",
          "type": "integer",
          "required": false,
          "description": "how long it waits for a signal, 5000 by default"
        }
      ],
      "returns": "the decoded protocol, address, and command",
      "status": "planned"
    },
    {
      "name": "ir.learn",
      "layer": "device",
      "category": "infrared",
      "description": "Learns a new IR signal (from a physical remote) and saves it under a given name, for later reuse.",
      "args": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "the name to save the signal under, e.g. tv_power"
        }
      ],
      "returns": "save confirmation",
      "status": "planned"
    },
    {
      "name": "ir.send",
      "layer": "device",
      "category": "infrared",
      "description": "Retransmits a previously saved IR signal (universal remote functionality).",
      "args": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "the name of the saved signal, e.g. tv_power"
        }
      ],
      "returns": "transmission confirmation",
      "status": "planned"
    },
    {
      "name": "ir.queue",
      "layer": "device",
      "category": "infrared",
      "description": "Adds one IR code to the list pending transmission. Queueing is separate from transmitting because a CFP v1 frame cannot carry a whole code list: one frame queues one code.",
      "args": [
        {
          "name": "protocol",
          "type": "string",
          "required": true,
          "description": "IR protocol name, e.g. NEC, Samsung32, SIRC, RC5"
        },
        {
          "name": "address",
          "type": "string",
          "required": true,
          "description": "device address, hexadecimal, e.g. 0x07"
        },
        {
          "name": "command",
          "type": "string",
          "required": true,
          "description": "command byte, hexadecimal, e.g. 0x02"
        }
      ],
      "returns": "how many codes are queued",
      "status": "implemented",
      "agent_visible": false
    },
    {
      "name": "ir.bruteforce",
      "layer": "device",
      "category": "infrared",
      "description": "Starts transmitting the queued IR codes, one every 250 ms, showing a progress bar on the Flipper's screen. Returns as soon as the run has started, not when it finishes: the run lasts several seconds, far longer than the client's read timeout. The user stops it by pressing the middle (OK) button on the device when the appliance reacts.",
      "args": [
        {
          "name": "label",
          "type": "string",
          "required": false,
          "description": "what is being bruteforced, shown on the device screen; no spaces"
        }
      ],
      "returns": "start confirmation and the number of queued codes",
      "status": "implemented",
      "agent_visible": false
    },
    {
      "name": "ir.status",
      "layer": "device",
      "category": "infrared",
      "description": "How far the bruteforce has got: state (running/stopped/idle), how many codes have been sent, and how many were queued. 'stopped' means the user pressed OK on the device, i.e. the appliance responded to the code just sent.",
      "args": [],
      "returns": "state, codes sent, codes queued",
      "status": "implemented",
      "agent_visible": false
    },
    {
      "name": "ir.reset",
      "layer": "device",
      "category": "infrared",
      "description": "Clears the queued IR codes, so the next run starts from a clean slate.",
      "args": [],
      "returns": "clear confirmation",
      "status": "implemented",
      "agent_visible": false
    },
    {
      "name": "nfc.read",
      "layer": "device",
      "category": "nfc",
      "description": "Waits for an NFC tag/card brought close to the Flipper and returns its UID and type.",
      "args": [
        {
          "name": "timeout_ms",
          "type": "integer",
          "required": false,
          "description": "how long it waits for a card, 5000 by default"
        }
      ],
      "returns": "UID, card type (Mifare, NTAG, etc.)",
      "status": "planned"
    },
    {
      "name": "nfc.watch",
      "layer": "device",
      "category": "nfc",
      "description": "Monitors, over a period of time, all NFC tags/cards that come within range of the module.",
      "args": [
        {
          "name": "duration_ms",
          "type": "integer",
          "required": true,
          "description": "monitoring duration in milliseconds"
        }
      ],
      "returns": "a list of detected card UIDs/types, in chronological order, with timestamps",
      "status": "planned"
    },
    {
      "name": "nfc.emulate",
      "layer": "device",
      "category": "nfc",
      "description": "Emulates a previously saved NFC card (the Flipper behaves like that card towards a reader).",
      "args": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "the name of the saved card"
        }
      ],
      "returns": "emulation start confirmation",
      "status": "planned"
    },
    {
      "name": "rfid.read",
      "layer": "device",
      "category": "rfid",
      "description": "Reads a low-frequency RFID card (125 kHz) brought close to the Flipper.",
      "args": [
        {
          "name": "timeout_ms",
          "type": "integer",
          "required": false,
          "description": "how long it waits for a card, 5000 by default"
        }
      ],
      "returns": "card ID, protocol (EM4100, HID, etc.)",
      "status": "planned"
    },

    {
      "name": "wifi.board_info",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Reports the presence and firmware of the Wi-Fi dev board - an ESP32 flashed with Marauder firmware, attached to the Flipper's GPIO/UART header - together with the Marauder version and hardware model. Call this first whenever the user asks anything about Wi-Fi or Bluetooth, to confirm the board is attached before attempting any wifi.* or ble.* operation. If no board is present it returns the error 'wifi_board_not_connected'.",
      "args": [],
      "returns": "board presence, Marauder firmware version, hardware model",
      "status": "implemented"
    },
    {
      "name": "wifi.set_channel",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Locks the Wi-Fi radio to a specific 2.4 GHz channel (1-14). Scans and sniffs only see traffic on the current channel, so set the channel of a target access point (from wifi.list_ap) before sniffing its clients or capturing its handshake. Rejects channels outside 1-14 with 'invalid_channel'.",
      "args": [
        { "name": "channel", "type": "integer", "required": true, "description": "2.4 GHz channel, 1 to 14" }
      ],
      "returns": "the channel the radio is now tuned to",
      "status": "implemented"
    },
    {
      "name": "wifi.scan_ap",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Scans the surrounding 2.4 GHz Wi-Fi for access points (routers/hotspots) and stores what it finds in the board's access-point list. Passive: it only listens to the beacons networks already broadcast, it does not transmit. Run this before wifi.list_ap, and before selecting a target for any attack. A scan runs for a short window and then returns how many access points were captured.",
      "args": [
        { "name": "duration_ms", "type": "integer", "required": false, "description": "how long to listen, 8000 by default" }
      ],
      "returns": "how many access points were captured",
      "status": "implemented"
    },
    {
      "name": "wifi.scan_station",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Scans for client stations (phones, laptops, IoT devices) associated with the access points already captured, and stores them in the station list. Passive listening only. Run wifi.scan_ap first so there are access points to attribute the clients to. A station is what you target for a client-specific deauthentication.",
      "args": [
        { "name": "duration_ms", "type": "integer", "required": false, "description": "how long to listen, 8000 by default" }
      ],
      "returns": "how many client stations were captured",
      "status": "implemented"
    },
    {
      "name": "wifi.list_ap",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Lists the access points captured by the last wifi.scan_ap. Each entry is one token 'index;SSID;BSSID;channel;rssi;encryption' (fields separated by semicolons, no spaces). The index is the handle used by wifi.select_ap to choose a target. Use this to show the user which networks are in range so they can pick one, instead of acting on all of them blindly.",
      "args": [],
      "returns": "one token per access point: index, SSID, BSSID, channel, RSSI, encryption",
      "status": "implemented"
    },
    {
      "name": "wifi.list_station",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Lists the client stations captured by the last wifi.scan_station. Each entry is one token 'index;MAC;ap_index;rssi'. The index is the handle used by wifi.select_station.",
      "args": [],
      "returns": "one token per station: index, MAC, associated access-point index, RSSI",
      "status": "implemented"
    },
    {
      "name": "wifi.select_ap",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Marks one or more access points from the list as the current target for a subsequent attack (wifi.attack_deauth, wifi.attack_beacon_clone). The selector is an index ('2'), a range ('0-3'), a comma list ('0,4,5') or 'all'. Selecting a target does nothing on its own; it only decides what the next attack will act on. Prefer selecting the specific network the user named over 'all'.",
      "args": [
        { "name": "targets", "type": "string", "required": true, "description": "index, range like 0-3, comma list like 0,4,5, or the word all" }
      ],
      "returns": "how many access points are now selected",
      "status": "implemented"
    },
    {
      "name": "wifi.select_station",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Marks one or more client stations from the list as the current target, for a client-specific deauthentication. Same selector syntax as wifi.select_ap.",
      "args": [
        { "name": "targets", "type": "string", "required": true, "description": "index, range, comma list, or the word all" }
      ],
      "returns": "how many stations are now selected",
      "status": "implemented"
    },
    {
      "name": "wifi.clear_targets",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Clears the captured access-point and station lists and any current selection, so the next scan starts from a clean slate.",
      "args": [],
      "returns": "confirmation that the lists were cleared",
      "status": "implemented"
    },
    {
      "name": "wifi.sniff_beacon",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Captures raw beacon frames (the periodic advertisements access points broadcast). Passive monitoring, useful for surveying every network announcing itself, including hidden or spoofed ones. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that beacon sniffing has started",
      "status": "implemented"
    },
    {
      "name": "wifi.sniff_probe",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Captures probe-request frames: the messages client devices send out looking for known networks. Passive. It reveals which networks nearby phones/laptops have connected to before, and is the reconnaissance step behind a karma-style attack. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that probe sniffing has started",
      "status": "implemented"
    },
    {
      "name": "wifi.sniff_deauth",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Listens for deauthentication frames in the air - a defensive tool: it detects whether someone else is running a deauth attack nearby. Passive, transmits nothing. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that deauth-detection sniffing has started",
      "status": "implemented"
    },
    {
      "name": "wifi.sniff_pmkid",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Captures WPA/WPA2 handshake material (PMKID and EAPOL frames) from the selected or current-channel access point, and saves it for offline analysis. Passive capture of authentication traffic; often combined with a brief deauth of a client to force it to reconnect and produce a fresh handshake. Only capture handshakes for a network the user owns or is authorized to test. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that handshake capture has started, and how many handshakes were seen",
      "status": "implemented"
    },
    {
      "name": "wifi.sniff_pwnagotchi",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Detects nearby Pwnagotchi units by their characteristic beacon advertisements. Passive. A curiosity/recon tool. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that Pwnagotchi detection has started",
      "status": "implemented"
    },
    {
      "name": "wifi.sniff_raw",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Captures raw 802.11 packets on the current channel for later inspection or saving to a pcap. Passive. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that raw capture has started",
      "status": "implemented"
    },
    {
      "name": "wifi.sniff_esp",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Detects other ESP32/ESP8266 devices transmitting nearby. Passive recon. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that ESP detection has started",
      "status": "implemented"
    },
    {
      "name": "wifi.attack_deauth",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Sends 802.11 deauthentication frames against the currently selected access point(s) (wifi.select_ap) or station(s) (wifi.select_station), forcing the associated clients to disconnect. THIS IS AN ACTIVE ATTACK that disrupts a real network. Send it ONLY after the user has explicitly confirmed they own the target network or have authorization to test it, and only against a specific selected target - never a broad selection - unless the user asked for exactly that. Requires a selection first; returns 'no_target_selected' otherwise. Keeps transmitting until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that the deauth attack has started, and against how many targets",
      "status": "implemented"
    },
    {
      "name": "wifi.attack_deauth_all",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Deauthenticates EVERY access point currently in range, indiscriminately. This is a broad, disruptive attack that knocks nearby networks offline; it is almost never what a legitimate test needs. Strongly prefer wifi.attack_deauth against one selected target. Use only when the user explicitly asks to affect all networks AND confirms authorization for the whole environment (for example an isolated lab). Keeps transmitting until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that the broad deauth has started, and against how many access points",
      "status": "implemented"
    },
    {
      "name": "wifi.attack_beacon_random",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Floods the air with beacon frames advertising randomly named fake networks, so a swarm of bogus SSIDs appears in every device's Wi-Fi list. Active transmission that clutters the spectrum; confirm the user is authorized to transmit in this environment before starting. Runs until stopped with wifi.stop.",
      "args": [
        { "name": "count", "type": "integer", "required": false, "description": "how many fake SSIDs to broadcast, 20 by default" }
      ],
      "returns": "confirmation that beacon spam has started",
      "status": "implemented"
    },
    {
      "name": "wifi.attack_beacon_list",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Broadcasts beacon frames for every SSID in the user-defined list (built with wifi.ssid_add / wifi.ssid_generate), making those exact network names appear nearby. Active transmission; confirm authorization first. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that list-based beacon spam has started",
      "status": "implemented"
    },
    {
      "name": "wifi.attack_beacon_clone",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Broadcasts beacons impersonating the SSID of the currently selected access point (wifi.select_ap), creating a look-alike of that specific network - the beacon half of an evil-twin. Active transmission that impersonates someone's network; only against a network the user owns or is authorized to test, after explicit confirmation. Requires a selection; returns 'no_target_selected' otherwise. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that the clone beacon has started",
      "status": "implemented"
    },
    {
      "name": "wifi.attack_probe_flood",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Floods the channel with probe-request frames, which can stress access points that log or respond to them. Active transmission; confirm authorization first. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that the probe flood has started",
      "status": "implemented"
    },
    {
      "name": "wifi.attack_rickroll",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "A harmless-prank variant of beacon spam: broadcasts fake networks whose SSIDs spell out the lyrics of a well-known song. Still an active transmission that adds bogus networks to everyone's Wi-Fi list, so confirm the user is authorized to transmit here. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that the rickroll beacon spam has started",
      "status": "implemented"
    },
    {
      "name": "wifi.evil_portal",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Stands up a fake open access point with a captive-portal login page (an evil twin), so devices that connect are shown a page that can harvest whatever the page asks for. This is a phishing-capable attack and must only be used on the user's own network or in an explicitly authorized engagement, with clear confirmation. Runs until stopped with wifi.stop.",
      "args": [
        { "name": "ssid", "type": "string", "required": false, "description": "the network name to advertise; if omitted, the selected AP's SSID is cloned" }
      ],
      "returns": "confirmation that the evil portal has started",
      "status": "implemented"
    },
    {
      "name": "wifi.karma",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Answers the probe requests captured from nearby devices by impersonating whatever network name each device is looking for, luring it to connect. Active impersonation attack; authorized testing only, with explicit confirmation. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that the karma attack has started",
      "status": "implemented"
    },
    {
      "name": "wifi.wardrive",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Continuously logs every access point seen together with the GPS position it was seen at (requires a GPS module on the board), building a wardriving map on the SD card. Passive: it only records the beacons networks already broadcast. Runs until stopped with wifi.stop.",
      "args": [],
      "returns": "confirmation that wardriving has started",
      "status": "implemented"
    },
    {
      "name": "wifi.ssid_add",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Adds one SSID (network name) to the user-defined SSID list used by wifi.attack_beacon_list. Building the list does nothing on the air by itself. The name cannot contain spaces (CFP v1 limitation).",
      "args": [
        { "name": "ssid", "type": "string", "required": true, "description": "the network name to add, without spaces" }
      ],
      "returns": "confirmation, and the new size of the list",
      "status": "implemented"
    },
    {
      "name": "wifi.ssid_generate",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Fills the user-defined SSID list with a number of randomly generated network names, as raw material for wifi.attack_beacon_list. Does nothing on the air by itself.",
      "args": [
        { "name": "count", "type": "integer", "required": true, "description": "how many random SSIDs to generate" }
      ],
      "returns": "confirmation, and the new size of the list",
      "status": "implemented"
    },
    {
      "name": "wifi.ssid_list",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Lists the SSIDs currently in the user-defined list, one token per name.",
      "args": [],
      "returns": "the SSIDs in the list",
      "status": "implemented"
    },
    {
      "name": "wifi.gps_data",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Reads the current fix from a GPS module attached to the board (used by wifi.wardrive): latitude, longitude, satellite count. Returns a no-fix status if no GPS module is present or no satellites are locked. Passive.",
      "args": [],
      "returns": "latitude, longitude, satellites in view, or a no-fix status",
      "status": "implemented"
    },
    {
      "name": "wifi.save_pcap",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Writes the packets captured so far (from any sniff.* operation) to a .pcap file on the Flipper's SD card, for later analysis on a computer.",
      "args": [],
      "returns": "the path of the saved capture file",
      "status": "implemented"
    },
    {
      "name": "wifi.stop",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Stops whatever Wi-Fi operation is currently running (a scan, a sniff, or an attack). Marauder operations run continuously until stopped, so send this as soon as the user's goal is met, and always before starting a different one. Also stops any ongoing attack immediately.",
      "args": [],
      "returns": "confirmation that the current operation stopped",
      "status": "implemented"
    },

    {
      "name": "ble.scan",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Scans for Bluetooth Low Energy devices advertising nearby and stores them in the BLE device list. Passive listening. Run before ble.list. The board's Marauder firmware handles BLE in addition to Wi-Fi.",
      "args": [
        { "name": "duration_ms", "type": "integer", "required": false, "description": "how long to listen, 8000 by default" }
      ],
      "returns": "how many BLE devices were captured",
      "status": "implemented"
    },
    {
      "name": "ble.list",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Lists the BLE devices captured by the last ble.scan. Each entry is one token 'index;MAC;name;rssi'.",
      "args": [],
      "returns": "one token per device: index, MAC, advertised name, RSSI",
      "status": "implemented"
    },
    {
      "name": "ble.sniff_airtag",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Scans specifically for Apple AirTags and other Find My / BLE trackers advertising nearby - useful for detecting whether an unknown tracker is following someone. Passive. Runs until stopped with ble.stop.",
      "args": [],
      "returns": "confirmation that AirTag/tracker detection has started, and how many were seen",
      "status": "implemented"
    },
    {
      "name": "ble.spam_apple",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Broadcasts fake Apple proximity-pairing BLE advertisements (the 'sour apple' effect), making nearby iPhones/iPads show a stream of spurious pairing pop-ups. Active transmission that harasses nearby devices; confirm the user is authorized to transmit in this environment before starting. Runs until stopped with ble.stop.",
      "args": [],
      "returns": "confirmation that Apple BLE spam has started",
      "status": "implemented"
    },
    {
      "name": "ble.spam_android",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Broadcasts fake Google/Android Fast Pair BLE advertisements, making nearby Android phones show spurious pairing notifications. Active transmission; confirm authorization first. Runs until stopped with ble.stop.",
      "args": [],
      "returns": "confirmation that Android BLE spam has started",
      "status": "implemented"
    },
    {
      "name": "ble.spam_samsung",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Broadcasts fake Samsung Galaxy buds/watch BLE advertisements, triggering spurious pairing pop-ups on nearby Samsung devices. Active transmission; confirm authorization first. Runs until stopped with ble.stop.",
      "args": [],
      "returns": "confirmation that Samsung BLE spam has started",
      "status": "implemented"
    },
    {
      "name": "ble.spam_windows",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Broadcasts fake Microsoft Swift Pair BLE advertisements, triggering spurious pairing notifications on nearby Windows machines. Active transmission; confirm authorization first. Runs until stopped with ble.stop.",
      "args": [],
      "returns": "confirmation that Windows BLE spam has started",
      "status": "implemented"
    },
    {
      "name": "ble.spam_all",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Broadcasts BLE pairing advertisements for all supported vendors at once (Apple, Android, Samsung, Windows) - the most disruptive BLE-spam mode, affecting every nearby phone and laptop. Active, broad harassment; use only with explicit authorization for the whole environment. Runs until stopped with ble.stop.",
      "args": [],
      "returns": "confirmation that all-vendor BLE spam has started",
      "status": "implemented"
    },
    {
      "name": "ble.spam_airtag",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "offensive",
      "description": "Broadcasts many fake Apple Find My (AirTag) advertisements at once, so nearby Apple devices see a cloud of bogus trackers. Active transmission; confirm authorization first. Runs until stopped with ble.stop.",
      "args": [],
      "returns": "confirmation that AirTag spoofing has started",
      "status": "implemented"
    },
    {
      "name": "ble.detect_flipper",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Detects other Flipper Zero devices advertising over BLE nearby. Passive recon. Runs until stopped with ble.stop.",
      "args": [],
      "returns": "confirmation that Flipper detection has started, and how many were seen",
      "status": "implemented"
    },
    {
      "name": "ble.wardrive",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Continuously logs every BLE device seen together with its GPS position (requires a GPS module), building a Bluetooth wardriving map on the SD card. Passive. Runs until stopped with ble.stop.",
      "args": [],
      "returns": "confirmation that BLE wardriving has started",
      "status": "implemented"
    },
    {
      "name": "ble.stop",
      "layer": "device",
      "category": "ble",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Stops whatever BLE operation is currently running (a scan, a tracker sniff, or a spam). Send it as soon as the user's goal is met, and before starting a different BLE operation.",
      "args": [],
      "returns": "confirmation that the current BLE operation stopped",
      "status": "implemented"
    },
    {
      "name": "marauder.reboot",
      "layer": "device",
      "category": "wifi",
      "requires": "wifi_dev_board",
      "impact": "passive",
      "description": "Reboots the ESP32 Wi-Fi board running Marauder. Useful when the board has become unresponsive or is stuck in a previous operation that wifi.stop did not clear.",
      "args": [],
      "returns": "confirmation that the board is rebooting",
      "status": "implemented"
    },

    {
      "name": "agent.check_frequency_activity",
      "layer": "agent",
      "category": "agent",
      "subagent": "scanner",
      "description": "Delegates to the scanner subagent the monitoring of a frequency over several successive readings, in order to establish whether anything is actually transmitting on it. Use this instead of subghz.rssi whenever the question is about activity rather than about one instantaneous value: a single reading cannot distinguish an empty frequency from a transmitter that happens to be silent at that instant. It takes longer, since it performs several measurements, and it returns an interpreted conclusion together with all the readings behind it.",
      "uses": ["subghz.rssi"],
      "task": "Establish whether there is real radio activity on the requested frequency. Take several readings of the signal level, spaced out, and judge by the whole set: the maximum reached, how much the values vary, and whether anything stands out above the background noise. If no frequency was given, use the common ISM frequencies (433.92 MHz and 868.35 MHz).",
      "args": [
        { "name": "frequency", "type": "integer", "required": false, "description": "the frequency of interest, in Hz; if absent, the common ISM frequencies are used" },
        { "name": "readings", "type": "integer", "required": false, "description": "how many successive readings to take, 4 by default" }
      ],
      "returns": "an interpreted conclusion about the activity, plus the raw readings it rests on",
      "status": "implemented"
    },
    {
      "name": "agent.research_session_log",
      "layer": "agent",
      "category": "agent",
      "subagent": "analyst",
      "description": "Delegates to the analyst subagent the researching of the session log: every command executed on the device so far, with its arguments, results and errors. Use this for questions about what has already been done ('what have we measured so far?', 'which commands failed?', 'was this frequency checked before?'). The analyst has no access to the device, so it can only answer from the recorded history, never with a new measurement.",
      "uses": [],
      "task": "Research the session log below and answer the question asked. Rely exclusively on what the log contains, and state explicitly what it does not contain but would be needed for a complete answer.",
      "args": [
        { "name": "question", "type": "string", "required": true, "description": "what should be established from the session history" }
      ],
      "returns": "an answer drawn from the session log, with the gaps in it stated explicitly",
      "status": "implemented"
    },
    {
      "name": "agent.wifi_recon",
      "layer": "agent",
      "category": "agent",
      "subagent": "wifi_recon",
      "description": "Delegates to the wifi_recon subagent a full passive survey of the Wi-Fi (and optionally BLE) environment: it confirms the board, scans for access points and their clients, reads the lists, and reports back an interpreted picture - how many networks, which are open vs encrypted, which are busiest, and which stand out as plausible targets for the user's stated goal. Use this instead of calling the individual scan/list commands yourself whenever the user asks an open question about 'what is around' or 'find the network X', since it takes several successive scans and interprets the whole. The subagent is passive by design: it has NO attack tools and can only listen, so it can never start a deauth or spam on its own - any offensive action stays your decision, taken with the user, afterwards.",
      "uses": ["wifi.board_info", "wifi.scan_ap", "wifi.scan_station", "wifi.list_ap", "wifi.list_station", "ble.scan", "ble.list"],
      "task": "Survey the Wi-Fi environment and report an interpreted picture of it. Confirm the Wi-Fi board is present, scan for access points and their client stations, read the lists, and summarise: how many networks, their encryption, which channels are busy, which clients are active, and - in light of what the main agent asked for - which access points or stations are the plausible targets. Do not attack anything; you have no tools to do so. If a specific network was named, say whether it was found and give its index, channel and encryption.",
      "args": [
        { "name": "goal", "type": "string", "required": false, "description": "what the survey is for, e.g. 'find the network called Office_WiFi' or 'general overview'" }
      ],
      "returns": "an interpreted survey of the environment, with the captured lists behind it",
      "status": "implemented"
    },
    {
      "name": "agent.explain_subghz_capture",
      "layer": "agent",
      "category": "agent",
      "description": "Translates the raw result of a Sub-GHz capture into natural language: what type of device is likely the source of the signal (gate/car remote, sensor, weather station, etc.), based on the frequency, the detected protocol, and a purpose-built database of frequency-protocol-device correspondences, rather than just displaying the raw code as the stock Flipper application does.",
      "uses": [
        "subghz.read"
      ],
      "args": [
        {
          "name": "frequency",
          "type": "integer",
          "required": true,
          "description": "the frequency to probe, in Hz"
        }
      ],
      "returns": "a natural-language explanation + the confidence level of the identification",
      "status": "planned"
    },
    {
      "name": "agent.nfc_session_summary",
      "layer": "agent",
      "category": "agent",
      "description": "Runs nfc.watch for the specified duration and builds a natural-language summary of the session: how many distinct cards/tags were seen, which one repeated, and a guess about the usage context (e.g. 'it looks like the phone interacted 3 times with the same reader, possibly a transit pass'). This answers exactly the project's initial idea: to show what a phone's NFC connected with.",
      "uses": [
        "nfc.watch"
      ],
      "args": [
        {
          "name": "duration_ms",
          "type": "integer",
          "required": true,
          "description": "the duration of the monitoring session"
        }
      ],
      "returns": "a text summary + a structured list of the events",
      "status": "planned"
    },
    {
      "name": "agent.identify_ir_device",
      "layer": "agent",
      "category": "agent",
      "description": "Captures an unknown IR signal and tries to deduce what type of appliance/brand emitted it, based on the timing fingerprint of the protocol (NEC, RC5, Samsung, etc.), also offering an explanation of the reasoning, not just the protocol label.",
      "uses": [
        "ir.read"
      ],
      "args": [],
      "returns": "a guess about the type/brand of the appliance + the decoded protocol",
      "status": "planned"
    },
    {
      "name": "agent.ir_control",
      "layer": "agent",
      "category": "agent",
      "description": "Controls an IR appliance (TV, projector, audio system, air conditioner) from a request in natural language: 'turn off my tv', 'turn off this projector', 'volume up', 'next channel', 'mute the soundbar'. Works out from the request what kind of appliance it is and which function is wanted, selects only the codes that have a real chance of working, and transmits them one by one until the user presses the middle (OK) button on the Flipper to signal that the appliance reacted. Filtering by brand is the point: a Samsung TV is only ever sent Samsung codes, never other brands'. If the brand given is not in the database, the closest known name is used and the substitution is reported. If no brand is given, every brand of that appliance type is tried, which is when it behaves like a classic bruteforce. The Flipper's screen shows 'Bruteforcing IR' with a progress bar for the duration of the run. Codes come from a small built-in table first; after five unsuccessful attempts on the same appliance and button it searches an online database of thousands of real remotes automatically. Set search_online to do that immediately, which is what to do when the user says it did not work.",
      "uses": [
        "ir.queue",
        "ir.bruteforce",
        "ir.status",
        "ir.reset"
      ],
      "args": [
        {
          "name": "request",
          "type": "string",
          "required": true,
          "description": "the user's request, in their own words, e.g. 'turn off my samsung tv'"
        },
        {
          "name": "brand",
          "type": "string",
          "required": false,
          "description": "the appliance brand, if the user mentioned or implied one, e.g. samsung, lg, sony, epson"
        },
        {
          "name": "device_type",
          "type": "string",
          "required": false,
          "description": "tv, projector, audio or ac; pass it explicitly on follow-up requests ('volume down') where the appliance is known from earlier in the conversation but not named again"
        },
        {
          "name": "function",
          "type": "string",
          "required": false,
          "description": "power, volume_up, volume_down, mute, channel_up, channel_down or input; inferred from the request when omitted"
        },
        {
          "name": "search_online",
          "type": "boolean",
          "required": false,
          "description": "search the online IR database (IRDB) for this attempt instead of the built-in codes. Set it when the user reports that a previous attempt did not work, or asks for more codes to be tried. Requires a brand: the database is far too large to search without one. Slower than the built-in table, since the codes are fetched over the network."
        }
      ],
      "returns": "which codes were selected and why, where they came from (built-in table or the online database), how many were sent, and whether the user confirmed that the appliance responded",
      "status": "implemented"
    },
    {
      "name": "agent.remember",
      "layer": "agent",
      "category": "agent",
      "description": "Saves one durable fact to your persistent memory, so you still know it in future sessions after this conversation ends. Use it for things the user would expect you to remember across time: the devices they own and their brands ('the user's TV is a Samsung'), a stated preference, or a lasting, notable finding ('433.92 MHz is active at this location'). Do NOT store secrets, passwords, one-off values, or anything the user asked to keep private. Keep each fact to one short, self-contained sentence. The facts you already remember are listed in your system instruction, so check there before saving a duplicate.",
      "args": [
        { "name": "fact", "type": "string", "required": true, "description": "one short sentence to remember, e.g. 'the user's TV is a Samsung'" }
      ],
      "returns": "confirmation that the fact was stored, or that it was already remembered",
      "status": "implemented"
    },
    {
      "name": "agent.confirm_authorized_action",
      "layer": "agent",
      "category": "agent",
      "description": "A mandatory safety gate before any device command that transmits/acts upon the real world (subghz.replay, nfc.emulate, ir.send, and every offensive Wi-Fi/BLE command: wifi.attack_*, wifi.evil_portal, wifi.karma, ble.spam_*): the agent explicitly asks the user to confirm that they own the target or are authorized to test it, before sending the command. It executes nothing by itself, it only validates the intent.",
      "uses": ["subghz.replay", "nfc.emulate", "ir.send", "wifi.attack_deauth", "wifi.attack_deauth_all", "wifi.attack_beacon_random", "wifi.attack_beacon_list", "wifi.attack_beacon_clone", "wifi.attack_probe_flood", "wifi.attack_rickroll", "wifi.evil_portal", "wifi.karma", "ble.spam_apple", "ble.spam_android", "ble.spam_samsung", "ble.spam_windows", "ble.spam_all", "ble.spam_airtag"],
      "args": [
        {
          "name": "action",
          "type": "string",
          "required": true,
          "description": "the name of the device command about to be sent"
        }
      ],
      "returns": "confirmation or refusal, with the reason",
      "status": "planned"
    }
  ]
}
